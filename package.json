{
  "name": "distributed-todo-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "broker": "pnpm --filter broker run dev",
    "compile": "pnpm --recursive run compile",
    "c": "pnpm run compile",
    "compile:watch": "pnpm run compile && pnpm --recursive --parallel --stream run compile:watch",
    "cw": "pnpm run compile:watch",
    "test": "pnpm --recursive --stream run test",
    "t": "pnpm run test"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "stream-http": "^3.1.0",
    "url": "^0.11.0"
  }
}
